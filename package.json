{
  "name": "llm-secret-interceptor",
  "version": "0.0.0",
  "description": "HTTPS proxy that intercepts and masks secrets in LLM communications",
  "private": true,
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^25.0.0"
  }
}
